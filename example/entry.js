import React from 'react';
import ReactDOM from 'react-dom';
import Button from '../src/Button';

main();

function main() {
  ReactDOM.render(<Button>test</Button>, document.getElementById('app'));
};
